I created this app as a GUI to interact with a local database. I wanted to add
and edit possible Rails interview questions I find on the internet into a database without using the
command line directly. 
